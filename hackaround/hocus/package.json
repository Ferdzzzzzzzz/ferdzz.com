{
  "name": "hocus",
  "version": "1.0.0",
  "description": "Revise Hocuspocus backend",
  "type": "module",
  "module": "./dist/index.mjs",
  "scripts": {
    "build": "node build.js",
    "dev": "miniflare --live-reload --debug --port 4001",
    "types:check": "tsc && tsc -p test/tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.1.1",
    "esbuild": "^0.13.13",
    "miniflare": "^2.0.0",
    "prettier": "^2.4.1",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "@hocuspocus/server": "^1.0.0-alpha.91",
    "yjs": "^13.5.24"
  }
}
